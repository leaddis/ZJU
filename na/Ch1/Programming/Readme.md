# Equation solver
All programs are in the src folder, and files and images are in the report folder.

### build
In the terminal, typing "make" will build the executable files for B-E in the bin folder, and typing "make doc" will compile a PDF file in the report folder.